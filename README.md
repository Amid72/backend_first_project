# backend_first_project
# Learning Backend & Python Full Stack

This repository tracks my journey learning backend development.

## Project 1: Hello World FastAPI
A simple API that returns a JSON greeting.

### How to run:
1. Install dependencies: `pip install -r requirements.txt`
2. Start the server: `uvicorn main:app --reload`
3. Open: [http://127.0.0.1:8000](http://127.0.0.1:8000)
